import type { ThemeData } from "../../types";
export declare const makeSkeletonStyles: ({ tokens }: Pick<ThemeData, "tokens">) => {
    MuiSkeleton: {
        styleOverrides: {
            root: {
                backgroundColor: string;
            };
        };
    };
};
//# sourceMappingURL=skeleton.d.ts.map