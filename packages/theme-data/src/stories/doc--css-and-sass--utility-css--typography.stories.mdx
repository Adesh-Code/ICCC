import { Meta, Story, Canvas } from "@storybook/addon-docs";
import Typography from "@iccc/typography";

export const Container = (props) => (
  <Typography
    {...props}
    style={{
      backgroundColor: "#eee",
      border: "1px dashed #666"
    }}
  />
);

<Meta title="Digital HIG/CSS and SASS/Utility CSS/Typography" />

# Typography utility CSS

Digital HIG utility CSS provides a collection of typography classes based on [Tailwind CSS](https://tailwindcss.com/).

Note: Requires utility CSS to be present in the document. See [the CSS overview page](?path=/docs/iccc-css-and-sass-css-overview--page) for more details.

## How to use Tailwind CSS

Spacing utility classes are generated with a `dhig-` prefix. Please use the Tailwind documentation and adjust classnames accordingly. For example:

For example, in Tailwind docs:

`align-baseline` vertical-align: baseline;

Becomes:

```html
<div class="dhig-align-baseline">
  This div will have "baseline" vertical alignment
</div>
```

## Examples

A few selected examples and common use cases. Press the "Show code" button on the right of each example to view the implementation code. For full Tailwind CSS documentation, see links below.

### Text align

<Canvas>
  <Story name="Text align">
    <Container className="dhig-text-left">This text is left-aligned</Container>
    <Container className="dhig-text-center">
      This text is center-aligned
    </Container>
    <Container className="dhig-text-right">
      This text is right-aligned
    </Container>
    <Container className="dhig-text-justify">
      This text is using the "justify" alignment, which is better demonstrated
      with a longer text sample. Lorem ipsum dolor sit amet, consectetur
      adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore
      magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco
      laboris nisi ut aliquip ex ea commodo consequat.
    </Container>
  </Story>
</Canvas>

### Text transform

<Canvas>
  <Story name="Text transform">
    <Container className="dhig-uppercase">
      This element has uppercase text
    </Container>
    <Container className="dhig-lowercase">
      This element has lowercase text
    </Container>
    <Container className="dhig-capitalize">
      This element is using the "capitalize" casing
    </Container>
    <Container className="dhig-normal-case">
      This element preserves the original casing
    </Container>
  </Story>
</Canvas>

### Vertical align

<Canvas>
  <Story name="Vertical align">
    <Container className="dhig-align-baseline">
      Use <code>dhig-align-baseline</code> to align the baseline of an element
      with the baseline of its parent.
    </Container>
    <Container className="dhig-align-top">
      Use <code>dhig-align-top</code> to align the top of an element and its
      descendants with the top of the entire line.
    </Container>
    <Container className="dhig-align-middle">
      Use <code>dhig-align-middle</code> to align the middle of an element with
      the baseline plus half the x-height of the parent.
    </Container>
    <Container className="dhig-align-bottom">
      Use <code>dhig-align-bottom</code> to align the bottom of an element and
      its descendants with the bottom of the entire line.
    </Container>
    <Container className="dhig-align-text-top">
      Use <code>dhig-align-text-top</code> to align the top of an element with
      the top of the parent element’s font.
    </Container>
    <Container className="dhig-align-text-bottom">
      Use <code>dhig-align-text-bottom</code> to align the bottom of an element
      with the bottom of the parent element’s font.
    </Container>
  </Story>
</Canvas>

## Tailwind CSS documentation

| Module         | Documentation                               | Description                                               |
| -------------- | ------------------------------------------- | --------------------------------------------------------- |
| Text align     | https://tailwindcss.com/docs/text-align     | Utilities for controlling an element's text alignment     |
| Text transform | https://tailwindcss.com/docs/text-transform | Utilities for controlling an element's text transform     |
| Vertical align | https://tailwindcss.com/docs/vertical-align | Utilities for controlling an element's vertical alignment |
